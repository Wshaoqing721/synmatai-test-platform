# App
HOST=0.0.0.0
PORT=8000
DEBUG=False

# PostgreSQL (via docker-compose)
# Start DB: docker compose up -d
DATABASE_URL=postgresql+asyncpg://synmatai:synmatai@localhost:5432/agent_test_platform

# Agent backend
AGENT_API_BASE_URL=http://localhost:20244/api
AGENT_API_TIMEOUT=30.0
